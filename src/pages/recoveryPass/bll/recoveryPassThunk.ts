import { ThunkType } from '../../../main/bll/store';
import { RecoveryPassActionsType } from './recoveryPassActions';

export const loginTC = (): ThunkType<RecoveryPassActionsType> => async (
  dispatch,
) => {};
